[pytest]
# Configuración por defecto de pytest para theia-landing
# Fuerza ejecución paralela en todos los núcleos de CPU y salida compacta
addopts = -n auto -q --tb=short
testpaths = tests
python_files = test_*.py
python_functions = test_*
