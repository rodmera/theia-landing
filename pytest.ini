[pytest]
# Configuración por defecto de pytest para theia-landing
# Fuerza ejecución paralela (-n 4) y salida compacta
addopts = -n 4 -q --tb=short
testpaths = tests
python_files = test_*.py
python_functions = test_*
