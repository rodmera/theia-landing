[pytest]
# Configuración por defecto de pytest para theia-landing (concurrencia controlada para 35W TDP)
addopts = -n 2 -q --tb=short
testpaths = tests
python_files = test_*.py
python_functions = test_*
